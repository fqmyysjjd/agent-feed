# Project Milestones

This file defines the current implementation milestone route for Agent Feed.

## Owns

This file owns the current implementation phase route and scope sequencing for
Agent Feed development.

## Read When

Read this before planning scope, sequencing multi-step implementation,
release-facing work, or deciding whether a task belongs in the current product
phase.

## Evidence

1. `README.md`: public product positioning and install surface.
2. `docs/`: public protocol, template, and usage documentation.
3. `.github/workflows/`: release and CI automation.
4. `tests/`: implemented behavior coverage.

## Default Route

1. M0: Working CLI prototype and standard template.
2. M1: Robust validation and Python/Node verification profiles.
3. M2: Codex, Claude, Cursor, and Windsurf adapters.
4. M3: Brownfield project discovery and guided initialization.
5. M4: CI integration, update/migration flow, and release packaging.

CLI command behavior, template responsibility model, and generated file layout are serial integration surfaces unless explicitly split with disjoint write sets.
